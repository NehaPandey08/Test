Review this PR end-to-end. Do not just skim the diff, reason about it against the actual codebase and the linked Jira ticket.

1. JIRA REQUIREMENT MAPPING
   - Find the Jira ticket ID referenced in the PR title, description, or branch name.
   - If you have Jira access via MCP, fetch the ticket's acceptance criteria. If not, ask me to paste them.
   - For each acceptance criterion, state whether this PR fully satisfies it, partially satisfies it, or doesn't address it at all.
   - Flag any changes in the PR that are NOT explained by the ticket (scope creep, unrelated refactors, leftover debug code).

2. CORRECTNESS & LOGIC
   - Walk through each changed file and explain what changed and why.
   - Identify logic errors, off-by-one issues, incorrect null/empty handling, and broken edge cases.
   - For mapping/transformation logic (ISO 8583, ISO 20022, NCM/Nexo field mappings), verify field-level correctness: correct field tags, correct data types, correct optional/mandatory handling per the spec.

3. UNUSED / DEAD CODE
   - Flag any newly added methods, variables, imports, config entries, or DTO fields that are never referenced elsewhere in the changed or existing code.
   - Flag any newly added dependencies that aren't actually used.
   - Flag leftover commented-out code or TODOs without a ticket reference.

4. CORRECT FIELD / API USAGE
   - Check that any new or modified fields match existing naming, typing, and validation conventions used elsewhere in ModelHub.
   - Check for correct use of existing utility/helper methods instead of reimplementing logic that already exists in the codebase.
   - Flag any hardcoded values that should be config-driven or enum-based.

5. GOOD PRACTICES
   - Error handling: are exceptions swallowed, logged, or propagated appropriately?
   - Test coverage: are there tests for the new logic? Do existing tests still cover the changed behavior? Are there missing edge-case tests?
   - Naming and readability against our CLAUDE.md conventions (if present).
   - Security: input validation, injection risks, sensitive data (PANs, tokens) not logged in plaintext.
   - Backward compatibility: could this break existing consumers of ModelHub's APIs/mappings?

6. REUSABILITY & DESIGN PATTERNS
   - Identify any logic in this PR that is generic enough to be extracted into a reusable utility/service, rather than staying local to this use case. Explain what other ModelHub use cases (mapping generation, validation, other standards like ISO 8583/20022/NCM) could benefit from it.
   - Scan the existing codebase for design patterns already in use (e.g. Strategy, Factory, Builder, Adapter, Chain of Responsibility, Template Method) and note whether this PR follows or breaks those established conventions.
   - For any complex conditional logic, duplicated code, or tightly coupled classes in this PR, identify whether a design pattern would genuinely simplify it. Name the specific pattern and briefly explain how it would apply.
   - For each pattern suggestion, give an honest cost/benefit call: is the current code simple enough that introducing a pattern would be over-engineering, or does the complexity/reuse potential justify it? State your recommendation explicitly, don't just list options.
   - If a pattern is recommended, note whether it's a "fix now" (cheap, high value) or a "flag for later" (valid but out of scope for this PR).

7. SUMMARY
   - A short pass/fail-style verdict per Jira acceptance criterion.
   - A prioritized list of issues (blocker / should-fix / nit).
   - A short list of reusability/pattern recommendations, separated into "worth doing now" vs "worth a backlog ticket."
   - Anything you're uncertain about that I should verify manually.

Use `gh pr view 202 --comments` to check if a review was already posted before starting. Cite specific file paths and line numbers for every issue. 
